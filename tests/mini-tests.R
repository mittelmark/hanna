library(hanna)

x = 2 + 2

if (x != 4) {
    stop("Error: Something strange happened!!")
}

#W = hgraph$graph()
#
#if (ncol(W) == 6) {
#    stop("Error: Something strange happened in creating the graph!")
#}
